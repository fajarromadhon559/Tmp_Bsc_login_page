import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tmp_examp_login',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
